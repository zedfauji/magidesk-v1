<Page
    x:Class="Magidesk.Presentation.Views.SwitchboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Magidesk.Presentation.ViewModels"
    xmlns:dtos="using:Magidesk.Application.DTOs"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Actions -->
        <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,24">
            <TextBox PlaceholderText="Search tickets..." Width="300">
                <TextBox.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="14"/>
                        <TextBlock Text="Search"/>
                    </StackPanel>
                </TextBox.Header>
            </TextBox>

            <ComboBox Header="Filter Status" SelectedIndex="0" Width="200">
                <ComboBoxItem Content="All Open Tickets"/>
                <ComboBoxItem Content="Unpaid"/>
                <ComboBoxItem Content="Paid"/>
            </ComboBox>

            <Button Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Bottom">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="New Ticket"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Tickets Grid -->
        <GridView Grid.Row="1" ItemsSource="{x:Bind ViewModel.OpenTickets, Mode=OneWay}" SelectionMode="Single">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="dtos:TicketDto">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="12" Width="280" Height="160">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="8,4">
                                    <TextBlock Text="{Binding TicketNumber}" Foreground="White" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="{x:Bind TableName}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Margin="0,12">
                                <TextBlock Text="{x:Bind OwnerName}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="{x:Bind CreatedAt}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                <TextBlock Text="{x:Bind Status}" Margin="0,4,0,0" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </StackPanel>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding TotalAmount, Converter={StaticResource CurrencyConverter}}" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="Open" Command="{Binding ElementName=RootPage, Path=ViewModel.OpenTicketCommand}" CommandParameter="{x:Bind Id}"/>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
