<ContentDialog
    x:Class="Magidesk.Presentation.Views.VoidTicketDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Magidesk.Presentation.ViewModels"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d"
    Title="Void Ticket"
    PrimaryButtonText="Void Ticket"
    CloseButtonText="Cancel"
    PrimaryButtonCommand="{Binding VoidCommand}"
    PrimaryButtonCommandParameter="{Binding ElementName=VoidDialog}"
    x:Name="VoidDialog">

    <ContentDialog.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <x:String x:Key="CurrencyFormat">{0:C}</x:String>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </ContentDialog.Resources>

    <Grid RowSpacing="16" Width="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Ticket Info -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Ticket Details" Style="{StaticResource LabelStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Ticket #" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="{Binding Ticket.TicketNumber}" FontWeight="Bold"/>
                <TextBlock Text="|" Margin="8,0"/>
                <TextBlock Text="Total:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="{Binding Ticket.TotalAmount.Amount, Converter={StaticResource StringFormatConverter}, ConverterParameter={StaticResource CurrencyFormat}}" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>

        <!-- Void Reason -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Reason for Voiding" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding VoidReasons}" 
                      SelectedItem="{Binding SelectedReason, Mode=TwoWay}"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Select a reason..."/>
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       FontSize="12" 
                       Margin="0,4,0,0"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Waste Option -->
        <CheckBox Grid.Row="2" 
                  Content="Waste Items (Return to Inventory)" 
                  IsChecked="{Binding IsWasted, Mode=TwoWay}"
                  ToolTipService.ToolTip="If unchecked, items may be retained for future use or tracking."/>

        <!-- Warning -->
        <InfoBar Grid.Row="3"
                 Severity="Warning"
                 Title="Warning"
                 Message="This action cannot be undone. All items and payments will be voided."
                 IsOpen="True"
                 IsClosable="False"/>

    </Grid>
</ContentDialog>
