<Page
    x:Class="Magidesk.Presentation.Views.PrintTemplatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Magidesk.Presentation.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:PrintTemplatesViewModel}">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <CommandBar DefaultLabelPosition="Right" Background="Transparent">
            <AppBarButton Icon="Add" Label="New Template" Command="{Binding AddCommand}"/>
            <AppBarButton Icon="Edit" Label="Edit" Command="{Binding EditCommand}" CommandParameter="{Binding SelectedTemplate}"/>
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding LoadCommand}"/>
        </CommandBar>

        <!-- List -->
        <ListView 
            Grid.Row="1"
            ItemsSource="{Binding Templates}"
            SelectedItem="{Binding SelectedTemplate, Mode=TwoWay}"
            Padding="12">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Style="{StaticResource TitleTextBlockStyle}"/>
                            <TextBlock Text="{Binding Type}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" 
                                   Text="{Binding IsSystem, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='System|Custom'}" 
                                   VerticalAlignment="Center" 
                                   Foreground="{StaticResource SystemControlForegroundAccentBrush}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Status -->
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="20" Foreground="Red" FontSize="14"/>
    </Grid>
</Page>
