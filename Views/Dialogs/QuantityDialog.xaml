<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Magidesk.Views.QuantityDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Magidesk.Presentation.ViewModels"
    mc:Ignorable="d"
    Title="Enter Quantity"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary">

    <Grid ColumnSpacing="12">
        <StackPanel Spacing="12">
             <!-- Display -->
            <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="12" CornerRadius="4">
                <TextBlock Text="{x:Bind ViewModel.DisplayText, Mode=OneWay}" 
                           FontSize="32" FontWeight="Bold" HorizontalAlignment="Right"/>
            </Border>
            
            <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" FontSize="12"/>

            <!-- Numpad -->
            <Grid RowSpacing="4" ColumnSpacing="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="7" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="7" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Column="1" Content="8" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="8" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Column="2" Content="9" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="9" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>

                <Button Grid.Row="1" Content="4" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="4" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Row="1" Grid.Column="1" Content="5" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="5" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Row="1" Grid.Column="2" Content="6" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="6" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>

                <Button Grid.Row="2" Content="1" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="1" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Row="2" Grid.Column="1" Content="2" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="2" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Row="2" Grid.Column="2" Content="3" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="3" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>

                <Button Grid.Row="3" Content="C" Command="{x:Bind ViewModel.ClearCommand}" HorizontalAlignment="Stretch" Height="60" FontSize="20" Background="LightSalmon"/>
                <Button Grid.Row="3" Grid.Column="1" Content="0" Command="{x:Bind ViewModel.AppendNumberCommand}" CommandParameter="0" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
                <Button Grid.Row="3" Grid.Column="2" Content="⌫" Command="{x:Bind ViewModel.BackspaceCommand}" HorizontalAlignment="Stretch" Height="60" FontSize="20"/>
            </Grid>
        </StackPanel>
    </Grid>
</ContentDialog>
