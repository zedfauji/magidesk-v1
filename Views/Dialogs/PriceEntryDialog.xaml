<ContentDialog
    x:Class="Magidesk.Views.Dialogs.PriceEntryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{x:Bind ViewModel.Title, Mode=OneWay}"
    PrimaryButtonText="OK"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{x:Bind ViewModel.ItemName}" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
        
        <!-- Display -->
        <Border Grid.Row="1" Background="White" BorderBrush="Gray" BorderThickness="1" Padding="8">
            <TextBlock Text="{x:Bind ViewModel.Price, Mode=OneWay}" 
                       FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
        </Border>

        <!-- Keypad -->
        <Grid Grid.Row="2" ColumnSpacing="4" RowSpacing="4">
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Height" Value="60"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Content="1" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="1"/>
            <Button Grid.Row="0" Grid.Column="1" Content="2" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="2"/>
            <Button Grid.Row="0" Grid.Column="2" Content="3" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="3"/>

            <Button Grid.Row="1" Grid.Column="0" Content="4" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="4"/>
            <Button Grid.Row="1" Grid.Column="1" Content="5" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="5"/>
            <Button Grid.Row="1" Grid.Column="2" Content="6" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="6"/>

            <Button Grid.Row="2" Grid.Column="0" Content="7" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="7"/>
            <Button Grid.Row="2" Grid.Column="1" Content="8" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="8"/>
            <Button Grid.Row="2" Grid.Column="2" Content="9" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="9"/>

            <Button Grid.Row="3" Grid.Column="0" Content="C" Command="{x:Bind ViewModel.ClearCommand}" Background="OrangeRed" Foreground="White"/>
            <Button Grid.Row="3" Grid.Column="1" Content="0" Command="{x:Bind ViewModel.AppendDigitCommand}" CommandParameter="0"/>
            <Button Grid.Row="3" Grid.Column="2" Content="⌫" Command="{x:Bind ViewModel.BackspaceCommand}"/>
        </Grid>
    </Grid>
</ContentDialog>
