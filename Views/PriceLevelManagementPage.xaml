<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Magidesk.Presentation.Views.PriceLevelManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Bind ViewModel.Title}" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <!-- Toolbar -->
        <CommandBar DefaultLabelPosition="Right" Grid.Row="1" Background="Transparent" IsOpen="True">
            <AppBarButton Icon="Add" Label="Add New Level" Command="{x:Bind ViewModel.AddCommand}"/>
            <AppBarButton Icon="Refresh" Label="Reload" Command="{x:Bind ViewModel.LoadDataCommand}"/>
        </CommandBar>

        <!-- Content -->
        <Grid Grid.Row="2" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/> 
                <ColumnDefinition Width="*"/>   
            </Grid.ColumnDefinitions>

            <!-- List View -->
            <Grid Grid.Column="0" Margin="0,0,12,0">
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" CornerRadius="4">
                    <ListView ItemsSource="{x:Bind ViewModel.PriceLevels, Mode=OneWay}" 
                              SelectedItem="{x:Bind ViewModel.SelectedPriceLevel, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="12">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="Default" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" Visibility="{Binding IsDefault, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="Inactive" Foreground="Red" Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Reverse}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>

            <!-- Detail View -->
            <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8" Padding="24">
                 <Grid>
                     <!-- Placeholder State -->
                     <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16" Visibility="{x:Bind ViewModel.IsEditing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Reverse}">
                         <FontIcon Glyph="&#xE70F;" FontSize="48" Foreground="Gray"/>
                         <TextBlock Text="Select a price level to edit" Foreground="Gray" Style="{StaticResource SubtitleTextBlockStyle}"/>
                     </StackPanel>

                     <!-- Edit Form -->
                     <StackPanel Spacing="16" Visibility="{x:Bind ViewModel.IsEditing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                         <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                         
                         <TextBox Header="Name" Text="{x:Bind ViewModel.EditingName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                         <TextBox Header="Description" Text="{x:Bind ViewModel.EditingDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                         <TextBox Header="Display Order" Text="{x:Bind ViewModel.EditingDisplayOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Number"/>
                         
                         <CheckBox Content="Is Active" IsChecked="{x:Bind ViewModel.EditingIsActive, Mode=TwoWay}"/>
                         <CheckBox Content="Is Default Price Level" IsChecked="{x:Bind ViewModel.EditingIsDefault, Mode=TwoWay}"/>
                         
                         <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,24,0,0">
                             <Button Content="Save Changes" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SaveCommand}"/>
                             <Button Content="Delete" Command="{x:Bind ViewModel.DeleteCommand}"/>
                         </StackPanel>
                     </StackPanel>
                 </Grid>
            </Border>
        </Grid>
        
        <!-- Busy Indicator -->
        <Grid Grid.RowSpan="3" Background="#80000000" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
             <ProgressRing IsActive="True" Width="60" Height="60"/>
        </Grid>
    </Grid>
</Page>
