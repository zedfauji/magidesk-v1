<Page
    x:Class="Magidesk.Presentation.Views.TableExplorerPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dto="using:Magidesk.Application.DTOs"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:TableStatusToBrushConverter x:Key="TableStatusConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{x:Bind ViewModel.Title}" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12"/>

        <!-- Search / Filter -->
        <AutoSuggestBox Grid.Row="1" 
                        PlaceholderText="Search table number..." 
                        Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        QueryIcon="Find"
                        Margin="0,0,0,24"
                        Width="300"
                        HorizontalAlignment="Left"/>

        <!-- Table List -->
        <ListView Grid.Row="2" 
                  ItemsSource="{x:Bind ViewModel.FilteredTables, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:TableDto">
                    <Grid Padding="12" Margin="0,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="4" Height="40" Background="{Binding Status, Converter={StaticResource TableStatusConverter}}" CornerRadius="2" Margin="0,0,16,0"/>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding TableNumber, ConverterParameter='Table {0}', Converter={StaticResource StringFormatConverter}}" FontWeight="Bold" FontSize="18"/>
                            <TextBlock Text="{Binding Capacity, ConverterParameter={0} Seats, Converter={StaticResource StringFormatConverter}}" Opacity="0.6"/>
                        </StackPanel>

                        <Button Grid.Column="2" 
                                Content="Select" 
                                Command="{Binding ElementName=RootPage, Path=ViewModel.SelectTableCommand}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource AccentButtonStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
