# Testing and Quality

Agents must generate or update tests where code behavior is changed.

## Rules
- All new public methods must have corresponding tests.
- UI interactions must be testable via view-model interactions or automated UI tests.
- Tests must assert expected behavior, not just compile.

## Meaningful Enforcement
Tests should be actionable, concise, and tied to functional change.
