<UserControl
    x:Class="Magidesk.Presentation.Controls.ToastNotificationHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Magidesk.Presentation.Controls"
    xmlns:services="using:Magidesk.Presentation.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ItemsControl 
        ItemsSource="{x:Bind ToastService.ActiveToasts, Mode=OneWay}"
        VerticalAlignment="Top"
        HorizontalAlignment="Right"
        Margin="0,80,24,0">
        
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Spacing="8" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="services:ToastNotification">
                <Border 
                    Background="{x:Bind BackgroundBrush, Mode=OneWay}"
                    CornerRadius="8"
                    Padding="16"
                    Width="320"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <FontIcon 
                            Grid.Column="0"
                            Glyph="{x:Bind Icon, Mode=OneWay}"
                            FontSize="20"
                            Foreground="White"
                            Margin="0,0,12,0"
                            VerticalAlignment="Top"/>
                        
                        <!-- Content -->
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock 
                                Text="{x:Bind Title, Mode=OneWay}"
                                FontWeight="SemiBold"
                                Foreground="White"
                                TextWrapping="NoWrap"
                                TextTrimming="CharacterEllipsis"/>
                            
                            <TextBlock 
                                Text="{x:Bind Message, Mode=OneWay}"
                                Foreground="White"
                                Opacity="0.9"
                                TextWrapping="Wrap"
                                MaxLines="3"
                                TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        
                        <!-- Dismiss Button -->
                        <Button 
                            Grid.Column="2"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4"
                            VerticalAlignment="Top"
                            Margin="8,0,0,0"
                            Click="DismissButton_Click"
                            Tag="{x:Bind}">
                            <FontIcon 
                                Glyph="&#xE711;"
                                FontSize="12"
                                Foreground="White"/>
                        </Button>
                    </Grid>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
