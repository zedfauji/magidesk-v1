<UserControl
    x:Class="Magidesk.Presentation.Controls.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <!-- Semi-transparent backdrop that covers the entire area -->
    <Grid 
        Background="{ThemeResource SystemControlAcrylicElementBrush}"
        Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
        IsHitTestVisible="{x:Bind IsLoading, Mode=OneWay}">
        
        <!-- Center content with progress indicator and message -->
        <StackPanel 
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16"
            Padding="32"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            MinWidth="300">
            
            <!-- Progress Ring -->
            <ProgressRing 
                IsActive="{x:Bind IsLoading, Mode=OneWay}"
                Width="48" 
                Height="48"
                HorizontalAlignment="Center"/>
            
            <!-- Loading Message -->
            <TextBlock 
                Text="{x:Bind LoadingMessage, Mode=OneWay}"
                Style="{StaticResource SubtitleTextBlockStyle}"
                TextAlignment="Center"
                TextWrapping="Wrap"
                MaxWidth="400"
                Visibility="{x:Bind LoadingMessage, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}"/>
            
            <!-- Cancel Button (optional) -->
            <Button 
                Content="Cancel"
                HorizontalAlignment="Center"
                Visibility="{x:Bind IsCancellable, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                Command="{x:Bind CancelCommand, Mode=OneWay}"
                MinWidth="120"
                Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
