<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Magidesk.Presentation.Views.InventoryBulkEditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Magidesk.Presentation.ViewModels"
    mc:Ignorable="d"
    Title="Bulk Edit Inventory Items"
    PrimaryButtonText="Apply Changes"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <Grid Padding="24" MinWidth="600" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Edit stock quantities and reorder points for selected items"
            Style="{StaticResource BodyTextBlockStyle}"
            Margin="0,0,0,16"/>

        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.EditableItems, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:InventoryBulkEditRow">
                    <Grid Padding="0,12" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Text="{x:Bind Name}"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center"/>

                        <StackPanel
                            Grid.Column="1"
                            Spacing="4"
                            Margin="16,0,0,0">
                            <TextBlock
                                Text="Stock Quantity"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBox
                                Text="{x:Bind NewStockQuantity, Mode=TwoWay}"
                                InputScope="Number"
                                Padding="8,4"/>
                        </StackPanel>

                        <StackPanel
                            Grid.Column="2"
                            Spacing="4"
                            Margin="16,0,0,0">
                            <TextBlock
                                Text="Reorder Point"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBox
                                Text="{x:Bind NewReorderPoint, Mode=TwoWay}"
                                InputScope="Number"
                                Padding="8,4"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
