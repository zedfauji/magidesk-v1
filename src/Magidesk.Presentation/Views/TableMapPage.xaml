<Page
    x:Class="Magidesk.Presentation.Views.TableMapPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Magidesk.Presentation.ViewModels"
    xmlns:dto="using:Magidesk.Application.DTOs"
    xmlns:views="using:Magidesk.Presentation.Views"
    xmlns:controls="using:Magidesk.Presentation.Controls"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:TableStatusToBrushConverter x:Key="TableStatusConverter"/>
        <converters:TableDtoToStatusBrushConverter x:Key="TableDtoToStatusBrushConverter"/>
        <converters:TableSessionStatusToIconConverter x:Key="SessionStatusToIconConverter"/>
        <converters:TableStatusToTooltipConverter x:Key="TableStatusToTooltipConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <CommandBar DefaultLabelPosition="Right" Background="Transparent">
            <AppBarToggleButton Icon="Sync" Label="{Binding Localization[TM_RealTime], Mode=OneWay}" 
                              IsChecked="{x:Bind ViewModel.IsRealTimeEnabled, Mode=TwoWay}"
                              Command="{x:Bind ViewModel.ToggleRealTimeCommand}"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Refresh" Label="{Binding Localization[TM_Refresh], Mode=OneWay}" Command="{x:Bind ViewModel.RefreshTablesCommand}"/>
            
            <AppBarElementContainer>
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="{Binding Localization[TM_LastUpdate], Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Bind ViewModel.LastRefresh, Converter={StaticResource DateTimeToTimeConverter}}" 
                              Style="{StaticResource CaptionTextBlockStyle}" 
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
            </AppBarElementContainer>
        </CommandBar>

        <!-- Interaction Instructions -->
        <InfoBar Grid.Row="1"
                 IsOpen="True"
                 IsClosable="True"
                 Severity="Informational"
                 Title="Table Map Interactions"
                 Message="Click a table to start a session or view details. Right-click for more options. Drag servers onto tables to assign them."
                 Margin="12,8,12,8">
            <InfoBar.ActionButton>
                <HyperlinkButton Content="Learn More" NavigateUri="https://docs.magidesk.com/table-map"/>
            </InfoBar.ActionButton>
        </InfoBar>

        <!-- Map Area -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Width="{x:Bind ViewModel.CanvasWidth, Mode=OneWay}" 
                  Height="{x:Bind ViewModel.CanvasHeight, Mode=OneWay}"
                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <ItemsControl ItemsSource="{x:Bind ViewModel.Tables, Mode=OneWay}">
                    <!-- Panel is a Canvas for absolute positioning -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- Item Template: Enhanced Table Control -->
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="dto:TableDto">
                            <controls:EnhancedTableControl 
                                Table="{x:Bind}"
                                Width="{x:Bind Width}"
                                Height="{x:Bind Height}"
                                TableClicked="OnTableClicked"
                                TableRightClicked="OnTableRightClicked"
                                ServerAssigned="OnServerAssigned">
                                <controls:EnhancedTableControl.RenderTransform>
                                    <TranslateTransform X="{x:Bind X}" Y="{x:Bind Y}"/>
                                </controls:EnhancedTableControl.RenderTransform>
                            </controls:EnhancedTableControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <!-- Status Legend -->
        <Border Grid.Row="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Legend Title -->
                <TextBlock Grid.Column="0"
                          Text="Status Legend:"
                          FontWeight="SemiBold"
                          VerticalAlignment="Center"
                          Margin="0,0,16,0"/>

                <!-- Legend Items -->
                <StackPanel Grid.Column="1"
                           Orientation="Horizontal"
                           Spacing="24">
                    
                    <!-- Available -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="24" Height="24"
                               Background="#4CAF50"
                               BorderBrush="#2E7D32"
                               BorderThickness="2"
                               CornerRadius="4"/>
                        <TextBlock Text="Available" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Occupied -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="24" Height="24"
                               Background="#F44336"
                               BorderBrush="#C62828"
                               BorderThickness="2"
                               CornerRadius="4"/>
                        <TextBlock Text="Occupied" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Reserved -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="24" Height="24"
                               Background="#FFC107"
                               BorderBrush="#F57C00"
                               BorderThickness="2"
                               CornerRadius="4"/>
                        <TextBlock Text="Reserved" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Dirty -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="24" Height="24"
                               Background="#9E9E9E"
                               BorderBrush="#616161"
                               BorderThickness="2"
                               CornerRadius="4"/>
                        <TextBlock Text="Dirty" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Paused -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Border Width="24" Height="24"
                               Background="#FF9800"
                               BorderBrush="#E65100"
                               BorderThickness="2"
                               CornerRadius="4"/>
                        <TextBlock Text="Paused" VerticalAlignment="Center"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
