<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Magidesk.Presentation.Views.Dialogs.ConfirmationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Magidesk.Presentation.Views.Dialogs"
    xmlns:vm="using:Magidesk.Presentation.ViewModels.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{x:Bind ViewModel.Title, Mode=OneWay}"
    PrimaryButtonText="{x:Bind ViewModel.PrimaryButtonText, Mode=OneWay}"
    SecondaryButtonText="{x:Bind ViewModel.SecondaryButtonText, Mode=OneWay}"
    DefaultButton="Secondary">

    <StackPanel Spacing="16" MinWidth="400" MaxWidth="500">
        <!-- Warning InfoBar at top -->
        <InfoBar 
            IsOpen="True"
            IsClosable="False"
            Severity="{x:Bind ViewModel.InfoBarSeverity, Mode=OneWay}"
            Message="{x:Bind ViewModel.WarningMessage, Mode=OneWay}"/>
        
        <!-- Detail Message -->
        <TextBlock 
            Text="{x:Bind ViewModel.Message, Mode=OneWay}" 
            Style="{StaticResource BodyTextBlockStyle}"
            TextWrapping="Wrap"/>
        
        <!-- Detail Card for relevant information -->
        <Border 
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="12"
            Visibility="{x:Bind ViewModel.DetailCardVisibility, Mode=OneWay}">
            <ItemsControl ItemsSource="{x:Bind ViewModel.DetailItems, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:DetailItem">
                        <StackPanel Spacing="4" Margin="0,0,0,8">
                            <TextBlock 
                                Text="{x:Bind Label}" 
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock 
                                Text="{x:Bind Value}" 
                                FontWeight="SemiBold"
                                Style="{StaticResource BodyTextBlockStyle}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        
        <!-- Additional Details (if available) -->
        <TextBlock 
            Text="{x:Bind ViewModel.Details, Mode=OneWay}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap"
            Visibility="{x:Bind ViewModel.DetailsVisibility, Mode=OneWay}"/>
    </StackPanel>
</ContentDialog>