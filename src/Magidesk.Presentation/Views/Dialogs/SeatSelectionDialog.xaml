<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Magidesk.Views.Dialogs.SeatSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Assign Seat Number"
    PrimaryButtonText="Assign"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonCommand="{Binding ConfirmCommand}"
    SecondaryButtonCommand="{Binding CancelCommand}">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Text="{Binding Input, Mode=OneWay}" 
                 Header="Seat Number"
                 FontSize="32" 
                 FontWeight="Bold" 
                 HorizontalContentAlignment="Center"
                 IsReadOnly="True"/>

        <Grid Grid.Row="1" ColumnSpacing="8" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <Button Content="1" Command="{Binding AppendCommand}" CommandParameter="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="20"/>
            <Button Content="2" Command="{Binding AppendCommand}" CommandParameter="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1" FontSize="20"/>
            <Button Content="3" Command="{Binding AppendCommand}" CommandParameter="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="2" FontSize="20"/>

            <Button Content="4" Command="{Binding AppendCommand}" CommandParameter="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1" FontSize="20"/>
            <Button Content="5" Command="{Binding AppendCommand}" CommandParameter="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1" Grid.Column="1" FontSize="20"/>
            <Button Content="6" Command="{Binding AppendCommand}" CommandParameter="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1" Grid.Column="2" FontSize="20"/>

            <Button Content="7" Command="{Binding AppendCommand}" CommandParameter="7" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="2" FontSize="20"/>
            <Button Content="8" Command="{Binding AppendCommand}" CommandParameter="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="2" Grid.Column="1" FontSize="20"/>
            <Button Content="9" Command="{Binding AppendCommand}" CommandParameter="9" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="2" Grid.Column="2" FontSize="20"/>

            <Button Content="C" Command="{Binding ClearCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="3" Background="IndianRed" Foreground="White" FontSize="20"/>
            <Button Content="0" Command="{Binding AppendCommand}" CommandParameter="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="3" Grid.Column="1" FontSize="20"/>
        </Grid>
    </Grid>
</ContentDialog>
