<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Magidesk.Views.Dialogs.PizzaModifierDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Magidesk.Presentation.ViewModels.Dialogs"
    mc:Ignorable="d"
    Title="Pizza Configuration"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    RequestedTheme="Light">

    <Grid x:Name="RootGrid" MinWidth="500" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" 
                      HorizontalAlignment="Center" VerticalAlignment="Center" 
                      Canvas.ZIndex="10"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Groups, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:PizzaModifierGroupViewModel">
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{x:Bind Name}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{x:Bind Modifiers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="vm:PizzaModifierItemViewModel">
                                        <Grid Margin="0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Name and Price -->
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{x:Bind Price}" FontSize="12" Foreground="Gray"/>
                                            </StackPanel>

                                            <!-- Section Toggles -->
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                                <ToggleButton Content="Left" IsChecked="{x:Bind IsLeft, Mode=TwoWay}" Width="60"/>
                                                <ToggleButton Content="Whole" IsChecked="{x:Bind IsWhole, Mode=TwoWay}" Width="60"/>
                                                <ToggleButton Content="Right" IsChecked="{x:Bind IsRight, Mode=TwoWay}" Width="60"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Rectangle Height="1" Fill="LightGray" Margin="0,8"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentDialog>
