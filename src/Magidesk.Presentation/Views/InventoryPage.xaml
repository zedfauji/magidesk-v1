<Page
    x:Class="Magidesk.Presentation.Views.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <UserControl.Resources>
        <!-- Converters assumed available globally or via alias -->
    </UserControl.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{x:Bind ViewModel.Title}" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Manage ingredients and stock levels." Style="{StaticResource BodyTextBlockStyle}" Opacity="0.6"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- List -->
            <Grid Grid.Column="0" Margin="0,0,24,0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Items" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.InventoryItems}" SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Unit}" Opacity="0.6" FontSize="12"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding StockQuantity}" HorizontalAlignment="Right"/>
                                    <TextBlock Text="In Stock" Opacity="0.6" FontSize="10" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button Grid.Row="2" Command="{x:Bind ViewModel.AddItemCommand}" Content="Add Item" HorizontalAlignment="Stretch" Margin="0,12,0,0"/>
            </Grid>

            <!-- Details -->
            <Grid Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24"
                  Visibility="{x:Bind ViewModel.IsEditing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Spacing="16">
                    <TextBlock Text="Edit Inventory Item" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    
                    <TextBox Header="Name" Text="{x:Bind ViewModel.EditingName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBox Header="Unit (e.g. oz, kg, count)" Text="{x:Bind ViewModel.EditingUnit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Header="Stock Quantity" Text="{x:Bind ViewModel.EditingStock, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Number"/>
                        <TextBox Grid.Column="1" Header="Reorder Point" Text="{x:Bind ViewModel.EditingReorder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Number"/>
                    </Grid>

                    <TextBox Header="Adjustment Reason (Required for stock changes)" 
                             PlaceholderText="e.g. Broken, Wastage, Received Order"
                             Text="{x:Bind ViewModel.AdjustmentReason, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,24,0,0">
                        <Button Command="{x:Bind ViewModel.SaveCommand}" Content="Save Changes" Style="{StaticResource AccentButtonStyle}"/>
                        <Button Command="{x:Bind ViewModel.DeleteCommand}" Content="Delete" Style="{StaticResource DefaultButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16"
                        Visibility="{x:Bind ViewModel.IsEditing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Reverse}">
                <FontIcon Glyph="&#xE70F;" FontSize="48" Foreground="Gray"/>
                <TextBlock Text="Select an item to edit details" Foreground="Gray" Style="{StaticResource SubtitleTextBlockStyle}"/>
            </StackPanel>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}"/>
        </Border>
    </Grid>
</Page>
