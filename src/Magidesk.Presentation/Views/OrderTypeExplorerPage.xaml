<Page
    x:Class="Magidesk.Presentation.Views.OrderTypeExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Magidesk.Presentation.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Bind ViewModel.Title}" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <CommandBar DefaultLabelPosition="Right" Grid.Row="1" Background="Transparent" IsOpen="True">
            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind ViewModel.AddCommand}"/>
            <AppBarButton Icon="Refresh" Label="Reload" Command="{x:Bind ViewModel.LoadCommand}"/>
        </CommandBar>

        <Grid Grid.Row="2" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Order Types" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>

                <Border Grid.Row="1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" CornerRadius="4">
                    <ListView ItemsSource="{x:Bind ViewModel.OrderTypes, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedOrderType, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="12" Spacing="4">
                                    <TextBlock Text="{Binding Name}"/>
                                    <TextBlock Text="{Binding IsActive}" Opacity="0.6"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>

            <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8" Padding="24">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12"
                                Visibility="{x:Bind ViewModel.HasSelection, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Reverse}">
                        <FontIcon Glyph="&#xE8A5;" FontSize="48" Foreground="Gray"/>
                        <TextBlock Text="Select an order type to edit" Foreground="Gray" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    </StackPanel>

                    <ScrollViewer Visibility="{x:Bind ViewModel.HasSelection, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Spacing="16">
                            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}"/>

                            <TextBox Header="Name" Text="{x:Bind ViewModel.EditingName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                            <ToggleSwitch Header="Active" IsOn="{x:Bind ViewModel.EditingIsActive, Mode=TwoWay}"/>
                            <ToggleSwitch Header="Close on Paid" IsOn="{x:Bind ViewModel.EditingCloseOnPaid, Mode=TwoWay}"/>
                            <ToggleSwitch Header="Allow Seat Based" IsOn="{x:Bind ViewModel.EditingAllowSeatBasedOrder, Mode=TwoWay}"/>
                            <ToggleSwitch Header="Allow Tips Later" IsOn="{x:Bind ViewModel.EditingAllowToAddTipsLater, Mode=TwoWay}"/>
                            <ToggleSwitch Header="Is Bar Tab" IsOn="{x:Bind ViewModel.EditingIsBarTab, Mode=TwoWay}"/>

                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,24,0,0">
                                <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SaveCommand}"/>
                                <Button Content="Deactivate" Command="{x:Bind ViewModel.DeactivateCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.RowSpan="3" Background="#80000000"
              Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="True" Width="60" Height="60"/>
        </Grid>
    </Grid>
</Page>
